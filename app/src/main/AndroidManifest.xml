<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <application
        android:name=".Application"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JellyfinDocumentsProvider"
        tools:targetApi="31">

        <activity
            android:name=".ServerWizardActivity"
            android:exported="false"
            android:label="@string/title_activity_server_wizard"
            android:theme="@style/Theme.JellyfinDocumentsProvider" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/title_activity_main"
            android:theme="@style/Theme.JellyfinDocumentsProvider">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <provider
            android:authorities="arne.jellyfindocumentsprovider"
            android:name=".provider.DocumentsProvider"
            android:exported="true"
            android:grantUriPermissions="true"
            android:permission="android.permission.MANAGE_DOCUMENTS"
            android:icon="@mipmap/ic_launcher"
            >
            <!-- This meta allows Poweramp to identify tracks provider and enables it for Poweramp -->
<!--            <meta-data android:name="com.maxmpz.PowerampTrackProvider" android:value="true"/>-->

            <intent-filter>
                <action android:name="android.content.action.DOCUMENTS_PROVIDER" />
            </intent-filter>
        </provider>
    </application>

</manifest>